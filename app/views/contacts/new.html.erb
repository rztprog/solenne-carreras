<div class="form-part">
    <%= form_for @contact, html: { class: "form-contact" } do |f| %>

    <% if @contact.errors.any? %>
        <div id="error_explanation" class="">
            <div class=""><%= pluralize(@contact.errors.count, "error") %> Interdiction d'enregistrement de cette page:</div>

            <ul class="">
                <% @contact.errors.each do |error| %>
                    <li class=""><%= error.full_message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <div class="form-samerow">
        <div class="form-row">
            <%= f.label "Prénom" %>
            <%= f.text_field :surname %>
        </div>
        <div class="form-row">
            <%= f.label "Nom" %>
            <%= f.text_field :name %>
        </div>
    </div>

    <div class="form-row">
        <%= f.label "Email" %>
        <%= f.email_field :email %>
    </div>

    <div class="form-row">
        <%= f.label "Message" %>
        <%= f.text_area :message %>
    </div>

    <div class="form-submit">
        <%= f.submit "Envoyer", class: "form-submit-button" %>
    </div>
    <% end %>
</div>
